# Browser automation for interactive login / online-order harvest.
playwright>=1.44

# HTTP client for the GraphQL API. The [http2] extra pulls in `h2`, which is
# required because api.py creates the client with http2=True.
httpx[http2]>=0.27

# Monthly-window date math in fetch.py.
python-dateutil>=2.9

# Parse printed receipt PDFs in the API-free `import` path (ingest.py).
pypdf>=4.0
